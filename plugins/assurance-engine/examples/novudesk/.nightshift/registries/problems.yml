# .nightshift/registries/problems.yml — NovuDesk PM problems
# ==========================================================
#
# DEFERRED for NovuDesk. The PM (evidence-grounded) lane is Phase 3 and stays OFF until
# manifest.evidence_sources is non-empty (support/churn/reviews/analytics connected) and
# the product has real signal. manifest.cadences.pm is `off`. Standing this lane up dry
# would produce invented problems, so the list is intentionally empty.
#
# Entries (when live) conform to ${CLAUDE_PLUGIN_ROOT}/schemas/registry-entry.yml
# (kind: problem, owner: product).
# ---------------------------------------------------------------------------

problems: []
