# .nightshift/registries/problems.yml — PM problem registry (TEMPLATE)
# ====================================================================
#
# Product-problem coverage for the PM (evidence-grounded) lane
# (kind: problem, owner: product). Each entry conforms to
# ${CLAUDE_PLUGIN_ROOT}/schemas/registry-entry.yml.
#
# DEFERRED BY DEFAULT. Do NOT seed this file until manifest.evidence_sources is
# non-empty (support/churn/reviews/analytics connected) AND the product has real signal.
# Standing the PM lane up dry produces INVENTED problems. Keep manifest.cadences.pm: off
# until then.
#
# When live: separate observed (ticket/churn frequency) from estimated (analytics reach)
# from strategic (partner/business weight); attach ONE confidence; produce briefs only —
# never reprioritize the live roadmap.
# ---------------------------------------------------------------------------

problems: []

# ---- Commented example problem entry (uncomment only once evidence_sources exist):
#
# problems:
#   - id:            PROB-01
#     title:         "Users abandon during plan upgrade — unclear pricing"
#     kind:          problem
#     area:          ["/billing/upgrade"]   # surface where the problem manifests
#     weight:        medium
#     interval_days: 30
#     owner:         product
#     signal_class:  observed     # observed | estimated | strategic
#     confidence:    medium
#     # last_reviewed / status / linear are (auto) — do not hand-edit.
